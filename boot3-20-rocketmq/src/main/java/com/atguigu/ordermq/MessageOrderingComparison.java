package com.atguigu.ordermq;

/**
 * 顺序消息 vs 非顺序消息 - 核心区别说明
 *
 * =====================================================================
 * 一、消息发送流程对比
 * =====================================================================
 *
 * 【顺序消息】
 * Producer → [根据hashKey选择队列] → Queue0 (订单1001的所有消息)
 *                                  → Queue1 (订单1002的所有消息)
 *                                  → Queue2 (订单1003的所有消息)
 *
 * 发送代码：
 * rocketMQTemplate.syncSendOrderly("topic", message, String.valueOf(orderId));
 *                                                     ↑
 *                                                  hashKey（关键！）
 *
 * 【非顺序消息】
 * Producer → [轮询/随机分配] → Queue0 (订单2001-创建, 订单2003-付款, ...)
 *                           → Queue1 (订单2001-付款, 订单2002-创建, ...)
 *                           → Queue2 (订单2002-推送, 订单2001-完成, ...)
 *
 * 发送代码：
 * rocketMQTemplate.syncSend("topic", message);
 *
 *
 * =====================================================================
 * 二、消息消费流程对比
 * =====================================================================
 *
 * 【顺序消息消费】
 * Queue0 → Consumer Thread-A → 订单1001: 创建 → 付款 → 推送 → 完成 (严格顺序)
 * Queue1 → Consumer Thread-B → 订单1002: 创建 → 付款 → 推送 → 完成 (严格顺序)
 * Queue2 → Consumer Thread-C → 订单1003: 创建 → 付款 → 推送 → 完成 (严格顺序)
 *
 * 特点：
 * - 每个队列使用独立线程串行消费
 * - 消费失败会阻塞该队列，直到消费成功
 * - 保证同一队列内消息的消费顺序
 *
 * 消费者配置：
 * @RocketMQMessageListener(
 *     consumeMode = ConsumeMode.ORDERLY  ← 关键配置
 * )
 *
 * 【非顺序消息消费】
 * Queue0 → Consumer Thread-1 → 订单2001-创建
 * Queue0 → Consumer Thread-2 → 订单2003-付款  (并发消费)
 * Queue1 → Consumer Thread-3 → 订单2001-付款
 * Queue2 → Consumer Thread-4 → 订单2002-推送
 * ...
 *
 * 特点：
 * - 多线程并发消费，不保证顺序
 * - 消费失败不会阻塞其他消息
 * - 吞吐量高
 *
 * 消费者配置：
 * @RocketMQMessageListener(
 *     consumeMode = ConsumeMode.CONCURRENTLY  ← 默认模式
 * )
 *
 *
 * =====================================================================
 * 三、关键代码对比
 * =====================================================================
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │ 顺序消息 - 生产者                                                │
 * ├─────────────────────────────────────────────────────────────────┤
 * │ SendResult result = rocketMQTemplate.syncSendOrderly(           │
 * │     "order-topic",                    // Topic                  │
 * │     message,                          // 消息内容               │
 * │     String.valueOf(orderId)           // hashKey: 订单ID       │
 * │ );                                                              │
 * │                                                                 │
 * │ 核心：使用 orderId 作为 hashKey                                 │
 * │ 效果：相同 orderId 的消息 → 同一个队列                          │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │ 顺序消息 - 消费者                                                │
 * ├─────────────────────────────────────────────────────────────────┤
 * │ @RocketMQMessageListener(                                       │
 * │     topic = "order-topic",                                      │
 * │     consumerGroup = "order-consumer-group",                     │
 * │     consumeMode = ConsumeMode.ORDERLY  // ← 顺序消费           │
 * │ )                                                               │
 * │ public class OrderedConsumer implements RocketMQListener<T> {   │
 * │     public void onMessage(T message) { ... }                    │
 * │ }                                                               │
 * │                                                                 │
 * │ 核心：ConsumeMode.ORDERLY                                       │
 * │ 效果：同一队列的消息按顺序消费                                   │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │ 非顺序消息 - 生产者                                              │
 * ├─────────────────────────────────────────────────────────────────┤
 * │ SendResult result = rocketMQTemplate.syncSend(                  │
 * │     "non-order-topic",                // Topic                  │
 * │     message                           // 消息内容               │
 * │ );                                                              │
 * │                                                                 │
 * │ 核心：不指定 hashKey                                            │
 * │ 效果：消息随机分配到不同队列                                     │
 * └─────────────────────────────────────────────────────────────────┘
 *
 * ┌─────────────────────────────────────────────────────────────────┐
 * │ 非顺序消息 - 消费者                                              │
 * ├─────────────────────────────────────────────────────────────────┤
 * │ @RocketMQMessageListener(                                       │
 * │     topic = "non-order-topic",                                  │
 * │     consumerGroup = "non-order-consumer-group",                 │
 * │     consumeMode = ConsumeMode.CONCURRENTLY  // ← 并发消费      │
 * │ )                                                               │
 * │ public class NonOrderedConsumer implements RocketMQListener<T> {│
 * │     public void onMessage(T message) { ... }                    │
 * │ }                                                               │
 * │                                                                 │
 * │ 核心：ConsumeMode.CONCURRENTLY (默认)                           │
 * │ 效果：多线程并发消费，不保证顺序                                 │
 * └─────────────────────────────────────────────────────────────────┘
 *
 *
 * =====================================================================
 * 四、使用场景选择
 * =====================================================================
 *
 * 【何时使用顺序消息】
 * ✓ 订单状态流转：创建 → 支付 → 发货 → 完成
 * ✓ 账户余额变更：需要按照时间顺序处理
 * ✓ 状态机流转：必须按照固定顺序执行
 * ✓ 业务日志记录：需要保证顺序性的操作日志
 *
 * 【何时使用非顺序消息】
 * ✓ 通知推送：短信、邮件等通知
 * ✓ 数据同步：可以并发处理的数据
 * ✓ 日志收集：普通日志记录
 * ✓ 统计分析：可以并发处理的数据分析
 *
 *
 * =====================================================================
 * 五、性能对比
 * =====================================================================
 *
 * 指标            │  顺序消息      │  非顺序消息
 * ───────────────┼───────────────┼──────────────
 * 发送性能        │  中等          │  高
 * 消费性能        │  低（串行）    │  高（并发）
 * 消息顺序        │  保证          │  不保证
 * 可靠性          │  高            │  高
 * 实现复杂度      │  中等          │  简单
 * 故障恢复        │  慢（阻塞）    │  快（不阻塞）
 *
 *
 * =====================================================================
 * 六、测试方法
 * =====================================================================
 *
 * 1. 启动 Spring Boot 应用
 * 2. 访问以下接口进行测试：
 *
 *    【顺序消息测试】
 *    GET http://localhost:8080/order/sendOrderedMessage
 *
 *    观察日志，同一订单的消息应该按顺序消费：
 *    订单1001: 创建订单 → 付款 → 推送 → 完成
 *
 *    【非顺序消息测试】
 *    GET http://localhost:8080/order/sendNonOrderedMessage
 *
 *    观察日志，同一订单的消息可能乱序消费：
 *    订单2001: 创建订单
 *    订单2002: 付款
 *    订单2001: 完成  ← 可能在"付款"之前被消费
 *
 *    【对比测试】
 *    GET http://localhost:8080/order/compare
 *
 *    同时测试两种模式，直观对比差异
 *
 *
 * =====================================================================
 * 七、常见问题
 * =====================================================================
 *
 * Q1: 顺序消息为什么比较慢？
 * A1: 因为同一队列的消息必须串行消费，前一条消息处理完才能处理下一条，
 *     导致并发度低。如果有100个不同的订单，理论上可以并发处理。
 *
 * Q2: 如何提高顺序消息的性能？
 * A2: 增加队列数量，确保有足够多的不同 hashKey（如不同的订单ID），
 *     这样可以提高并发度。
 *
 * Q3: 顺序消息消费失败怎么办？
 * A3: 会阻塞该队列的后续消息，需要保证消费逻辑的健壮性，或实现快速失败
 *     和重试机制。
 *
 * Q4: 非顺序消息会重复消费吗？
 * A4: 可能会，因此消费端需要保证幂等性（多次消费产生相同结果）。
 *
 * Q5: 能否局部顺序（只保证某些消息顺序）？
 * A5: 可以！这正是顺序消息的设计目的。通过 hashKey 将需要保证顺序的消息
 *     路由到同一队列，不同 hashKey 的消息可以并发处理。
 *
 * =====================================================================
 */
public class MessageOrderingComparison {
    // 这是一个文档类，仅用于说明，不包含可执行代码
}

