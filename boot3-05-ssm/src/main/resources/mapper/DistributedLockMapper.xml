<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.boot3.ssm.mapper.DistributedLockMapper">

    <!-- 插入锁记录，利用唯一索引保证互斥 -->
    <insert id="insertLock">
        INSERT INTO distributed_lock (lock_key, owner, expire_time)
        VALUES (#{lockKey}, #{owner}, #{expireTime})
    </insert>

    <!-- 释放锁：按 key + owner 删除，防止误删他人的锁 -->
    <delete id="deleteLock">
        DELETE FROM distributed_lock
        WHERE lock_key = #{lockKey}
          AND owner = #{owner}
    </delete>

    <!-- 清理已过期的锁 -->
    <delete id="deleteExpiredLocks">
        DELETE FROM distributed_lock
        WHERE expire_time &lt; NOW()
    </delete>

    <!-- 按 key 查询锁记录 -->
    <select id="getLock" resultType="com.atguigu.boot3.ssm.bean.DistributedLockRecord">
        SELECT id, lock_key, owner, expire_time, created_time
        FROM distributed_lock
        WHERE lock_key = #{lockKey}
    </select>

</mapper>
